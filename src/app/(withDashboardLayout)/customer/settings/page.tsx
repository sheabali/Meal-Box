import SettingsPage from '@/components/modules/settings/SettingsPage';
import React from 'react';

const page = () => {
  return (
    <div>
      <SettingsPage />
    </div>
  );
};

export default page;
